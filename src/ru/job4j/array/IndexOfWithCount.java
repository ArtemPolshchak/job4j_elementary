package ru.job4j.array;

public class IndexOfWithCount {
    public static int indexOf(char[] string, char c, int number) { //abcd
        int rsl = -1;
        char[] temp = new char[c];
        for (int index = 0; index < string.length; index++) { //0(a) < 4
            if (string[index] == temp[])
        }
        return rsl;
    }
}
